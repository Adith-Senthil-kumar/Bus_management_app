specVersion: "v1beta"
serviceId: "busbuddy"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "busbuddy-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
